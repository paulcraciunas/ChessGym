package com.paulcraciunas.screens.tools.dashboard.ui

object ToolsDashboardTags {
    private const val PREFIX = "tools_dashboard_"
    const val SCREEN = "${PREFIX}screen"

    object Cards {
        private const val CARD = "${PREFIX}card_"
        const val CLOCK = "${CARD}clock"
        const val ANALYSIS = "${CARD}analysis"
        const val IMPORT_GAME = "${CARD}import_game"
    }
}
